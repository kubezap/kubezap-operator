# EPIC-NNN: <Title>

**Status:** Backlog
**PI:** —

## Problem

What's broken, missing, or insufficient today — in terms of the operator's actual behavior, users (cluster operators, Flow authors, contributors), or the project's release goals, not an invented persona.

## Goal

What "done" looks like for this epic, in one or two sentences.

## Success Metric

Concrete and checkable — e.g. "`make test-e2e` passes with zero pre-existing failures," "OLM bundle passes `bundle validate` and `scorecard`," "the security checklist has no open P0 items."

## Related Design Docs

Links into `docs/design/` (via the index) and `docs/architecture.md`/`CLAUDE.md` — not a requirements catalog. Add a new `docs/design/*.md` record (via `/adr`) if this epic makes a non-obvious technical decision that doesn't already have one.

## Candidate Stories

Rough list, not yet sized/footprinted — that happens in `/groom-backlog`.

- [ ] ...
- [ ] ...

## Dependencies

- Depends on: EPIC-xxx / none
- Blocks: EPIC-xxx / none

## Notes

Open questions, owner decisions still needed, risks.
