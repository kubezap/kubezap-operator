# STORY-NNN: <Title>

**Epic:** EPIC-NNN — <epic title>
**Status:** Backlog
**Size:** XS | S | M | L — rough Claude-session length, not story points

## Description

What this story delivers and why — a sentence or two is enough; this isn't a persona-driven product, so skip the "As a ... I want ..." framing unless a CLI/API-consumer-facing behavior genuinely benefits from it.

## Acceptance Criteria

- [ ] ...
- [ ] ...

## File / Module Footprint

Paths this story is expected to create or modify. This is what `/plan-parallel` uses to detect conflicts with other stories — be specific (`internal/controller/executor_reconciler.go`, not "the controller"). If a story can't be footprinted accurately yet, it isn't groomed yet.

- `path/...`
- `path/...`

## Dependencies

- Depends on: STORY-xxx / none
- Blocks: STORY-xxx / none

## Notes

Design rationale, rejected alternatives, known gaps — link to a `docs/design/*.md` record via `/adr` if the decision is non-obvious enough to need one (per `planning/process/design-process.md`'s trigger list), otherwise keep it here.
