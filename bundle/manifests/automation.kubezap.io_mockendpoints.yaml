apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: mockendpoints.automation.kubezap.io
spec:
  group: automation.kubezap.io
  names:
    kind: MockEndpoint
    listKind: MockEndpointList
    plural: mockendpoints
    singular: mockendpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Mock endpoint path
      jsonPath: .spec.path
      name: PATH
      type: string
    - description: Total requests
      jsonPath: .status.requestCount
      name: REQUEST COUNT
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MockEndpointSpec defines the desired behavior of a mock HTTP
              endpoint.
            properties:
              maxRequestHistory:
                default: 20
                description: MaxRequestHistory is how many recent requests are retained.
                format: int32
                type: integer
              path:
                description: Path is the suffix path registered under /mock/
                type: string
              response:
                description: Response is a default response to return.
                properties:
                  body:
                    description: Optional body content.
                    type: string
                  delayMs:
                    description: Artificial delay in milliseconds before returning.
                    format: int32
                    type: integer
                  headers:
                    additionalProperties:
                      type: string
                    description: Optional headers to include.
                    type: object
                  statusCode:
                    default: 200
                    description: StatusCode is the HTTP status code to return.
                    format: int32
                    type: integer
                type: object
              responseSequence:
                description: ResponseSequence can be used to cycle through responses.
                items:
                  description: MockResponse defines the HTTP response returned by
                    the mock endpoint.
                  properties:
                    body:
                      description: Optional body content.
                      type: string
                    delayMs:
                      description: Artificial delay in milliseconds before returning.
                      format: int32
                      type: integer
                    headers:
                      additionalProperties:
                        type: string
                      description: Optional headers to include.
                      type: object
                    statusCode:
                      default: 200
                      description: StatusCode is the HTTP status code to return.
                      format: int32
                      type: integer
                  type: object
                type: array
            required:
            - path
            type: object
          status:
            description: MockEndpointStatus contains status information for the mock
              endpoint.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              recentRequests:
                items:
                  description: CapturedRequest stores details of a received request.
                  properties:
                    body:
                      type: string
                    bodyTruncated:
                      type: boolean
                    headers:
                      additionalProperties:
                        type: string
                      type: object
                    method:
                      type: string
                    path:
                      type: string
                    responseStatusCode:
                      format: int32
                      type: integer
                    timestamp:
                      format: date-time
                      type: string
                  required:
                  - method
                  - path
                  - timestamp
                  type: object
                type: array
              requestCount:
                format: int64
                type: integer
              url:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
