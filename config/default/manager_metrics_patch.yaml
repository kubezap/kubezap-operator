# This patch explicitly sets the metrics bind address (matches the :9090 default in code,
# but keeps it visible in the deployment manifest for clarity).
- op: add
  path: /spec/template/spec/containers/0/args/0
  value: --metrics-bind-address=:9090
