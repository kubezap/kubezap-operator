# secrets.yaml is intentionally excluded — create secrets manually before applying.
# See docs/guides/github-autolabel.md for step-by-step instructions.
resources:
  - integration.yaml
  - flow.yaml
  - trigger.yaml
