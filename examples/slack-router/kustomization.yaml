# secrets.yaml is intentionally excluded — create secrets manually before applying.
# See docs/guides/slack-router.md for step-by-step instructions.
resources:
  - flow.yaml
  - trigger.yaml
  - mockendpoints.yaml
