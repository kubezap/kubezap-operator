# Follow-ups

A running log of edge cases, deferred ideas, or "we should revisit this" notes discovered during implementation, review, or grooming — captured here the moment they're noticed so they don't get buried in a story/epic file nobody rereads. Triaged during `/checkpoint`.

Each entry: date discovered, source (which story/epic/conversation surfaced it), a short description, and status. Once triaged, update the status rather than deleting the entry — this file is also a record of what got noticed and what happened to it.

Status values: `Open` (not yet triaged) → `Routed: <EPIC-NNN/STORY-NNN>` (turned into real backlog work) or `Dropped: <reason>`.

This replaces `docs/tech-debt/pending-input-required.md`'s role — an owner-decision question gets logged here as `Open` and triaged the same way, rather than in a separate file with its own `<!-- BACKLOG-PROMPT -->` convention.

## Log

- **2026-09-12** — Source: project-management restructuring conversation. `docs/schedule.md`, `docs/groom-latest.md`, `docs/review-latest.md`, and `docs/tech-debt/` were internal working documents shipping in the same repo about to go public, with no decided mechanism for keeping them out.
  Status: `Routed: chore/pm-migration-2026-09-12` — resolved by this same migration: project-management content now lives under top-level `planning/`, a single directory that's straightforward to exclude from a public mirror or split to a private repo later, rather than needing to be carved out of `docs/`.
- **2026-09-12** — Source: post-dependabot-merge validation pass. Bumping `golangci-lint` v2.1.0 → v2.13.2 (forced by a separate dependabot go.mod bump to `go 1.26.0`, which v2.1.0 can't lint against) surfaced **176 new `goconst` findings across the repo that the old version never reported** — same code, 0 issues before the bump, so this is a linter-sensitivity/default change, not a code regression. 117 of 176 were in test files (HTTP methods, header names, fixture data) — excluded via a `.golangci.yml` path rule (`_test\.go`, `test/e2e/*`), matching this repo's existing `dupl`/`lll` test-exclusion precedent and its own prior finding that per-line goconst suppression just shifts which occurrence gets reported (see `docs/design/2026-09-11-typed-phase-enums.md`). The remaining **59 findings are real production code**, concentrated in `internal/controller/integration_controller.go` (22), `internal/controller/gateway_deployment.go` (14), and smaller counts across `internal/cli/*`, `internal/controller/{cron_scheduler,executor_mtls,executor_reconciler,flow_controller,flowrun_controller,resource_watcher,trigger_controller}.go`, `internal/gateway/{nats,webhook}/*.go` — mostly Kubernetes API/label conventions (`app.kubernetes.io/name`, `automation.kubezap.io`, `ServiceAccount`, `Role`, `create`, `/healthz`, `Ready`, `cron`, `resource`) that would genuinely benefit from named constants, unlike the test-file noise.
  Status: `Routed: EPIC-002/STORY-017` — triaged at the 2026-09-12 checkpoint. Given `EPIC-002` (Post-Release Hardening & Feature Backlog) rather than a new epic, since that slot was reserved but never populated and fits this exactly.
- **2026-09-12** — Source: post-dependabot-merge validation pass (same conversation as above). Full validation after ~20 dependabot PRs merged (`k8s.io/{api,apimachinery,client-go}` v0.33→v0.37, `sigs.k8s.io/controller-runtime` v0.21→v0.25, Go toolchain 1.25→1.26.6, `sarama`, `ginkgo`/`gomega`, `otel`, and all 6 Dockerfiles' `golang:1.25`→`1.27` base bump): found and fixed 3 real breaks (a `sarama.SyncProducer` mock missing 2 new interface methods; `golangci-lint` binary too old to lint `go 1.26` code, bumped v2.1.0→v2.13.2; `sigs.k8s.io/controller-runtime/pkg/scheme.Builder` deprecated, replaced with a local dependency-free equivalent) plus 2 rounds of `govulncheck`-flagged stdlib CVEs fixed by bumping `go.mod`'s `go` directive further (1.26.0 had 26 known CVEs already patched by 1.26.1, then 1.26.1 had 21 more patched by 1.26.6 — landed on 1.26.6, the highest version any current finding cited). `make test-e2e` re-run confirms **zero new failures**: same 17/45 ran, 13 passed, 4 failed (identical failure signature/root cause to the pre-existing SSRF-bypass-flag/metrics findings already tracked in `docs/design/2026-09-11-executor-egress-networkpolicy.md`'s addendum and `STORY-003-test-suite-value-review.md`), 14 Pending, 14 Skipped.
  Status: `Dropped: no action needed` — this entry exists purely as a record that the post-upgrade validation was done and found no regressions beyond the goconst item above (which is `Open`, tracked separately).
- **2026-09-12** — Source: STORY-005 (community health files, PR #180) merge verification. GitHub's own `community/profile` API shows `health_percentage: 100` post-merge, but the same response's `issue_template` field is `null` even though `.github/ISSUE_TEMPLATE/bug_report.yml` and `feature_request.yml` are both confirmed present at the correct path (verified via `gh api repos/.../contents/.github/ISSUE_TEMPLATE`).
  Status: `Open` — re-checked at the 2026-09-12 checkpoint (several PR cycles after the original merge): `issue_template` is still `null`. The indexing-lag theory is disproven — this looks like a real gap, most likely GitHub's community-profile check specifically wanting the older markdown template format rather than the YAML issue-form format used here. Owner decision (2026-09-12): log and defer, not urgent enough to investigate further right now.
- **2026-09-12** — Source: STORY-004 (GitHub Pages website, PR #184, merged). Building `docs/` with MkDocs (source root `docs_dir: docs`, `docs/design/` intentionally excluded per the story's own scope) surfaced pre-existing broken links in doc content that STORY-004 wasn't permitted to fix (footprint was the site generator/workflow only, not `docs/` content): `docs/architecture/flowrun-state-model.md` and `docs/guides/getting-started.md` each link outside `docs_dir` (to `planning/process/code-review-strategy.md` and `examples/order-router/README.md` respectively) and will 404 on the published site; `docs/architecture.md` links to `docs/design/scale-limitations.md`, which 404s because `design/` is deliberately excluded from the site. Also found a handful of in-page anchor links (e.g. `#trigger--flow-invocation`) that don't match MkDocs/Python-Markdown's actual generated heading slugs — cosmetic, page-internal only.
  Status: `Routed: EPIC-001/STORY-016` — triaged at the 2026-09-12 checkpoint as its own standalone story (STORY-001 was already closed, didn't fold into it).
- **2026-09-12** — Source: STORY-004 (PR #184) and STORY-006 (PR #183) merge verification. Both stories' code/content is merged, but each has one manual, admin-only repo-settings action still outstanding that no PR can perform: (1) STORY-004 — `gh api repos/kubezap/kubezap-operator/pages` returns 404, meaning the Pages source hasn't been set to "GitHub Actions" yet (Settings → Pages), so the merged deploy workflow has nothing to publish to until this happens; (2) STORY-006 — `gh api repos/kubezap/kubezap-operator` shows `has_discussions: false`, so the README's new "Getting Help" link currently points at a disabled feature (Settings → Features → Discussions, plus setting up starter categories per the story's own AC).
  Status: `Open` — owner decision (2026-09-12): both deliberately deferred until after the repo actually goes public, not an oversight or something to chase down now. Enabling Discussions/Pages on a still-private repo would be premature. Revisit as part of the actual public-launch sequence (natural fit: right after STORY-007's rollup gate passes and the repo is made public — see that story's Notes).
- **2026-09-12** — Source: STORY-009 (WebhookGatewayConfig CRD, PR #190) implementation. The pre-existing `+kubebuilder:webhook` marker on `internal/webhook/trigger_webhook.go`'s `SetupTriggerWebhook` is attached directly above/inside that function's doc comment rather than in its own free-floating comment block — the exact same "silently dropped by controller-gen" failure mode `CLAUDE.md` already documents for `+kubebuilder:rbac` markers, just never previously noticed for `+kubebuilder:webhook`. Confirmed empirically while building STORY-009's own webhook: `config/webhook/` didn't exist anywhere in the repo before this story, meaning the Trigger webhook's (and, by the same mechanism, the FlowRun webhook's) `ValidatingWebhookConfiguration` has never actually been generated by `make manifests`, despite both webhook *handlers* being registered and working correctly in `cmd/main.go` at the Go level.
  Status: `Routed: EPIC-002/STORY-018` — triaged at the 2026-09-12 checkpoint. (`CLAUDE.md`'s gotcha note was already updated to cover both marker types, in the same PR that logged this entry.)
- **2026-09-12** — Source: STORY-018 (webhook marker fix, PR #194) implementation. `internal/webhook/flowrun_webhook.go` has **no** `+kubebuilder:webhook` marker at all (not the same placement bug as Trigger's — there's nothing to reposition). Adding one is a design decision, not a mechanical fix: `failurePolicy` isn't derivable from existing code (Trigger uses `ignore`, `WebhookGatewayConfig` uses `fail`, and nothing in `flowrun_webhook.go` indicates which is intended for FlowRun).
  Status: `Open` — needs grooming into a story once the `failurePolicy` question is decided with the owner.
- **2026-09-12** — Source: STORY-014 (benchmark harness, PR #195) implementation, discovered while running `make install` against a fresh Kind cluster. `config/crd/kustomization.yaml`'s `resources:` list was missing `bases/automation.kubezap.io_webhookgatewayconfigs.yaml` (present since STORY-009, PR #190), so `make install` never actually installed the `WebhookGatewayConfig` CRD — every Trigger in a freshly-installed cluster failed to reconcile the webhook gateway with `no matches for kind "WebhookGatewayConfigList"`. STORY-014 worked around it in the benchmark harness by applying the generated bases file directly, out of that story's footprint.
  Status: `Dropped: fixed directly` — one-line kustomization fix, no design implications, no reason to hold it for a story. Fixed 2026-09-13 (added the missing `resources:` entry), verified via `kustomize build config/crd` now emitting all 5 CRDs including `webhookgatewayconfigs.automation.kubezap.io`. Still worth checking separately why the release-validation/e2e path didn't catch this (envtest-based `make test` doesn't exercise `config/crd/kustomization.yaml` at all) — noted for STORY-007's spec-drift re-check.
- **2026-09-13** — Source: STORY-015 (execution-latency benchmark writeup, EPIC-004). The full 200-run benchmark confirmed KubeZap's per-step dispatch latency is genuinely small (~5–10ms, from the controller's own `kubezap_step_duration_seconds` Prometheus histogram) — but `FlowRun.status.steps[].startTime`/`completionTime` (`metav1.Time`, whole-second resolution) can't show this at all: 594/600 step observations in this benchmark's raw per-run data read as an exact `0.000s`, which is indistinguishable from "the step recorded nothing" to anyone reading `kubectl get flowrun -o yaml`. If "faster/lighter than Pod-per-step engines" becomes a stated public differentiator (see `benchmarks/execution-latency/RESULTS.md`'s go/no-go), users should be able to see real per-step latency on the FlowRun object itself, not only via a one-off metrics-endpoint scrape — this needs either sub-second-precision timestamp fields on `StepRunStatus` or an explicit duration field populated from the same `time.Since()` value already computed in `internal/controller/flowrun_controller.go` for the Prometheus histogram.
  Status: `Open` — this is a CRD/status schema change (an actual product change, not a wording change), so it needs its own Epic/Story via `/groom-backlog` rather than being implemented inside STORY-015's writeup-only footprint.
- **2026-09-13** — Source: STORY-015 (execution-latency benchmark writeup). Found and fixed inline (since it blocked getting a valid result, per the story's own allowance for that): `benchmarks/execution-latency/run-benchmark.sh`'s `stop_sampler_and_finalize` passed the full resource-sample array to `jq -n --argjson` as a shell argument; at full 200-run scale (~2400 samples over the ~107-minute Argo arm) this exceeds Linux's per-argument `MAX_ARG_STRLEN` (128KiB), so `jq` fails with `Argument list too long` and silently produces an empty `argo-resource-samples.json`. Fixed by reading the samples/created/captured data via `--slurpfile`/`--rawfile` (file-based, not argv-based) instead of `--argjson`. See `benchmarks/execution-latency/RESULTS.md` §5 for the full account.
  Status: `Dropped: fixed inline during STORY-015` — no further action needed; logged here per this file's own convention of recording harness/tooling bugs found during a story even when already resolved, so the fix is visible outside the story's own diff.
